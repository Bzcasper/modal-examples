This runs:
```modal run trtllm_qwen.py```
but produces outputs with too many eos tokens, I'm stripping them out for now.


Working on install tensorrt_llm==0.17.0 which claims to have better Qwen support
WIP: ```modal shell install_trtllm17.py```
